#include "main.h"
/**
 * mul - function that multiplies two integers.
 * @a: variable that holds an integer
 * @b: varaiable that holds an integer
 * Return: 0 success
 */
int mul(int a, int b)
{
	int mul = a * b;

	return (mul);
}

