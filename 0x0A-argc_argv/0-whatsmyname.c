#include <stdio.h>
#include "main.h"
/**
 * main - argc and argv task
 * @argc: counter
 * @argv: vector for the string
 * Return: success
 */

int main(int argc, char *argv[])
{
	(void) argc;
	printf("%s\n", argv[0]);
	return (0);
}
