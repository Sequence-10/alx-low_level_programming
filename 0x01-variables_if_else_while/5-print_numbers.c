#include <stdio.h>
#include <stdlib.h>
#include <limits.h>
/**
 * main - the function that prints all single digit
 *
 * Return: 0
 */

int main(void)
{
	int m;

	for (m = 0; m < 10; m++)
		printf("%d", m);
	printf("\n");

	return (0);
}

